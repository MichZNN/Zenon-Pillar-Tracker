[Unit]
Description=Zenon Pillar Tracker Docker Compose stack
Documentation=https://github.com/MichZNN/Zenon-Pillar-Tracker/blob/main/INSTALL.md
Requires=docker.service
After=docker.service network-online.target
Wants=network-online.target

[Service]
Type=oneshot
WorkingDirectory=/srv/zenon-pillar-tracker
EnvironmentFile=-/srv/zenon-pillar-tracker/.deploy-image.env
Environment=COMPOSE_PROFILES=collector
ExecStartPre=/usr/bin/env docker compose --profile collector config --quiet
ExecStart=/usr/bin/env docker compose --profile collector up -d --remove-orphans
ExecReload=/usr/bin/env docker compose --profile collector up -d --remove-orphans
ExecStop=/usr/bin/env docker compose --profile collector down --remove-orphans
RemainAfterExit=yes
TimeoutStartSec=0
TimeoutStopSec=120

[Install]
WantedBy=multi-user.target
